{
  "name": "generators",
  "root": "libs/generators",
  "projectType": "library",
  "sourceRoot": "libs/generators",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --build tsconfig.lib.json"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit"
      }
    }
  },
  "tags": ["type:utils", "scope:generators"]
}
